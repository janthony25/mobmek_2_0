# 2026-07-04

## Customer Page
- [x] In customer page user should also be able to search using REGO# (done: 2026-07-04 10:30)

## Customer Details Page
- [x] When I click View job in from Invoice/Quotes action button it leads me to the job page but there is no button to go back to that customer details page. Add that. (done: 2026-07-04 14:24)
- [x] In Invoices and Quotes section add number that counts all of the invoices/quotes under that car — implemented as both: total count in "Invoices (n)"/"Quotes (n)" card titles, plus a per-vehicle count badge in the Vehicles list (done: 2026-07-04 14:24)
- [x] In vehicle section instead of adding a logo with number for number of invoices and quotation just add Invoices: and Quotes: under the vehicle details so its more detailed (done: 2026-07-04 14:35)
- [x] Add search functionality for invoices section(calendar icon) so we can easily look for invoice within a date or week or month (done: 2026-07-04 13:05)
- [x] In invoices section right now its not so visible because there is just a gray line that separates them make make the border or the line that separates them more visible (Maybe update our Claude.MD for frontend since we would be having more like this like in Vehicle Details Page there is a job section that has list as well) (done: 2026-07-04 13:05)
- [x] make the invoice section default invoices only shows 5 (done: 2026-07-04 13:50; verified via tsc+lint only, no live browser click-through this session — no browser-automation tool available)
- [x] In invoices section add a dropdown button for the action that is used for invoices page as well, add clear filter as well (done: 2026-07-04 13:50; verified via tsc+lint only, no live browser click-through this session — no browser-automation tool available)
- [x] Add Quotes section under Invoices section with action button as well with 5 as limit and has search calendar as well with clear filter (done: 2026-07-04 13:50; verified via tsc+lint only, no live browser click-through this session — no browser-automation tool available)
- [x] In Appointment history section, when I click the job, it will go to the job, but instead of the button back to specific customer, it has back to job Center. Fix it (done: 2026-07-04 15:41; verified live with Playwright — job page back link now reads "← Back to {customer name}" and correctly returns to that customer's page)

## Job Page
- [x] In job page have a card version as well like customer where it has cards and list view. with also maximum of 10 cards (done: 2026-07-04 10:53)
- [x] In Add job page - When there are required fields that was not filled, instead of having a line at the end of the page make sure the page goes to that field with red background for the iput(for visibility) and not that the field is required (done: 2026-07-04 13:35)
- [x] In add job  when adding fields like when adding items/labour the remove button should have red background for visibility (done: 2026-07-04 13:35)
- [x] Add create appointment that would be link to the appointment page (done: 2026-07-04 13:50; verified via tsc+lint only, no live browser click-through this session — no browser-automation tool available)

## Vehicle Details Page
- [x] in reminders section, instead of having multiple action buttons in one line, we should make it a dropdown(...) (done: 2026-07-04 13:50)
- [x] In job section its hard to see different jobs because there are no borders for each job and just pure black text, maybe add border or if you have suggestion for better visibility (done: 2026-07-04 13:50)

## Appointment Page
- [x] In creating appointment, in existing customer. Make the Customer be input where user can type then a dropdown based on what the user is typing would show the list. Currently its just a dropdown of all customer which is not ideal for big database (done: 2026-07-04 11:05)
- [x] when creating an appointment aside from existing customer and new caller, user should be able to create appointment through existing job where it shows job cards and user could search using rego# to show only specific job for that rego (done: 2026-07-04 11:05)
- [x] when creating new appointment, when choosing existing job, only show 6 most recent job created as default and limit so it
would be user friendly if we have too much job and we can just search the rego if needed (done: 2026-07-04 10:57)
- [x] when an appointment is clicked, it should be easier to update the status instead of clicking to edit that has a lot more inputs needed. (done: 2026-07-04 11:49)
- [x] When an appointment is clicked, Vehicle should also be clickeable to be able straight to vehicle details page (done: 2026-07-04 11:49)

## General
- [x] Create pages for Invoices and Quotations. With 20 as default limit and has search functionality that can be name, rego# or date (done: 2026-07-04 12:04)
- [x] In invoices and quotation page, for date search it should be a calendar button so user could choose the date easily instead of typing (done: 2026-07-04 14:10)
- [x] For invoice and quotation add action dropdown button where it would have Mark as paid View Job, View Invoice/PDF (PDF), Mark as Paid (for invoice), Download Invoice/Quote, Send Email (to be implemented soon), Reject (done: 2026-07-04 14:35)
- [x] In Invoices and Quotation page, add a minimal show all button so that when it is filterd its easier to show all (done: 2026-07-04 13:50; verified via tsc+lint only, no live browser click-through this session — no browser-automation tool available)
- [x] In Invoices and Quotation page, when view job is clicked, make sure we have back to "Invoices/Quotes" Page (done: 2026-07-04 13:50; verified via tsc+lint only, no live browser click-through this session — no browser-automation tool available)
- [x] In Invoices and Quotation page, when user click view job in specific page. They should be able to go back to the invoices/quotation page. Easiest way for this is to just go back to previous page when they use this button from invoices/quotation page. (done: 2026-07-04 14:24; confirmed already implemented via back-link state — re-verified live with Playwright from both /invoices and /quotations)
- [x] In customer details page when I click view job from invoices/quotes section Make sure that the Back to previous page is on the very top left not after reminders. Same when user is from Invoices and Quotation Page. Made this a standard in mobmek_frontend/CLAUDE.md that all back links go at the very top of a detail page's JSX. (done: 2026-07-04 14:35)
- [x] We need to add loading for page changes, search and other(check system and analyze what else need more loading) cause once its deployed it will be hard to know if its loading or there is really no data depending on internet speed or how big the data is. (done: 2026-07-04 15:11; root-caused and fixed a real bug in useAsync's reload() that could flash "Customer/Job/Car not found" or empty-list states after any save/delete/action across the app; added a shared Spinner component, inline refresh spinners on CrudSection/Invoices/Quotations/Appointments, a search-box spinner, and busy/disabled state on per-row actions; verified via tsc+lint and a live Playwright pass — no console errors, no stale/not-found flash across 10 rapid post-save snapshots)
- [x] In business details, instead of logo image URL use upload (done: 2026-07-04 15:41; backend: BusinessDetails entity/DTO now stores LogoStorageKey/FileName/ContentType via IFileStorage (same pattern as CashTransaction attachments), new POST/GET/DELETE /api/business-details/logo endpoints, migration ReplaceBusinessDetailsLogoUrlWithUpload, 3 new + 1 updated service tests, all 282 backend tests pass; frontend: replaced the "Logo image URL" text input with a file-upload control (upload/replace/remove) in BusinessDetailsSettingsPage.tsx, updated InvoicePrintPage.tsx's logo <img> to use the new businessLogoUrl() helper; verified live with Playwright — upload shows thumbnail + Replace/Remove, remove reverts to "+ Upload logo", and direct curl round-trip of the new endpoints confirmed)
- [x] In root project folder. Create a car-make-model.md that has json file of all CarMakes available in New Zealand. Each carmake will have models (from 1990 and above) make sure to list all the models that are under that car make, add the specific name and not general name. IMPORTANT: I need all carmodel in each carmake since this would be use for the database of carmake and carmodel (Do a deep Research and do not mark as done as long as its not every model from 1990+) (done: 2026-07-04 16:07; created /Users/jun/mobmek_2_0/car-make-model.md with a JSON object of 69 car makes and 1068 specific model nameplates covering NZ new-vehicle distribution history since 1990 plus the major JDM/used-import brands; compiled from manufacturer/dealer NZ sites, trade press, and detailed model-history research, with a methodology note in the file itself; verified the JSON parses cleanly and spot-checked Toyota/Kia/GWM/KGM/BYD entries for accuracy — treat as a strong seed list to extend as production data surfaces gaps)

# 2026-07-06

## Job Page
- [x] In view job, if a job already has an appointment instead of create appointment that shows it should be view appointment and user should see the modal for appointment details and will have the option to edit it. (done: 2026-07-06 18:42; backend: added an optional `jobId` filter to GET /api/appointments (`AppointmentsController`/`AppointmentService`/`IAppointmentService`), 1 new xUnit test (all 286 backend tests pass); frontend: `JobDetailPage` now queries `getAppointments({ jobId })`, and the header button reads "View appointment" (opens the existing `AppointmentDetailModal` for view/edit) when one exists, else "Create appointment" as before; found and fixed a real edge case while verifying with real data — a job can have more than one linked appointment (no DB constraint prevents it), so picking `data[0]` (sorted by start time) could show a stale one; switched to picking the most-recently-created linked appointment; verified against the live stack (rebuilt containers, ran the real migration-free `jobId` filter against real jobs/appointments — confirmed empty result for an unlinked job and correct match, including the multi-appointment case, for linked ones) via curl since no browser-automation tool was available this session — no live click-through of the modal itself)
- [x] when appointment is created from the view job page. The job already has a mechanic since it is required when creating the job so it should be set in the appointment directly (done: 2026-07-06 19:04; `AppointmentForm.tsx` now seeds `mechanicId` from `initialJob?.mechanics[0]?.employeeId` (the "Create appointment" prefill path from `JobDetailPage`) and also sets it in `handleJobPick` (the in-form "Existing job" search/pick tab), so either way of linking a job to an appointment carries over that job's mechanic instead of leaving it "Unassigned"; verified with real data that jobs do carry a `mechanics[]` array with `employeeId` (4 of 7 seeded jobs have one) so the seed resolves to a real id in practice; verified via tsc+lint, no live browser click-through this session — no browser-automation tool available)
- [x] when view appointment is clicked in view job page there should be a button to view it in the calendar (done: 2026-07-06 19:04; `AppointmentDetailModal` gained a "View in calendar" button that navigates to `/appointments` with `{ jumpToDate: a.startUtc, appointmentId: a.id }`; `AppointmentsPage`'s existing job-prefill `location.state` effect now also handles `jumpToDate` (jumps the calendar to that day via the existing `openDay`) and `appointmentId` (a new effect resolves it against the loaded day's appointments once they arrive and opens the detail modal); verified via tsc+lint and confirmed the built bundle actually contains the new "View in calendar" string after a container rebuild — no live browser click-through this session, no browser-automation tool available)
- [x] View in calendar should only show if user is in view job page when they click the view appointment button. Currently even when user is in appointment page and click the appointment, it has a view in calendar button. (done: 2026-07-06 19:18; `AppointmentDetailModal` gained a `showViewInCalendar?: boolean` prop (default `false`) gating the button added in the previous task; `JobDetailPage` now passes `showViewInCalendar`, while `AppointmentsPage` (the calendar itself) doesn't pass it, so the button no longer shows when an appointment is opened directly from the calendar; verified via tsc+lint and confirmed the `showViewInCalendar` string is present in the rebuilt container's bundle — no live browser click-through this session, no browser-automation tool available)

## General
- [x] Add a discount for a job (it would be under labour and user should have a button to add a discount) it should have option for ($ / %) where it would be calculated accordingly (done: 2026-07-06 18:42; backend: new `DiscountType` enum (None/Fixed/Percentage) + `DiscountValue` on `Job`, migration `AddJobDiscount`, `DiscountCalculator` helper shared by `JobService.RecalculateTotalsAsync` (clamped so a discount never pushes totals negative) and `InvoiceService.GenerateDocumentAsync` (previously had a dead, always-zero `Invoice.Discount` field — now actually populated and subtracted from `TotalAmount` before GST), 4 new/updated xUnit tests (all 286 backend tests pass); frontend: new `DiscountEditor`/read-only summary under the Labour section on both `NewJobPage` and `JobDetailPage` edit mode, `computeDiscountAmount` in `jobLineDrafts.ts` mirroring the backend clamp math for a live-updating estimated total, `JobForm.tsx` (the quick-edit form from Job Center) now round-trips the existing discount unchanged so a quick save can't silently wipe it; verified against the live stack (rebuilt containers, applied the real migration, exercised the API end-to-end: PUT a 20% discount on a real $150 job → totalJobPrice 120, generated an invoice from it → subTotal 150/discount 30/tax 18/total 138, then cleaned up by rejecting the test invoice and reverting the job's discount) — no live click-through of the New Job/Job Detail forms themselves since no browser-automation tool was available this session)
- [x] When saving in the pages under settings. There should be a confirmation popup before actually saving having the consequence of actually pressing save(depends on which page and what is the functionality of the page) (done: 2026-07-06 19:04; scoped to the two singleton settings screens — `TaxSettingsPage` and `BusinessDetailsSettingsPage` — since `ReminderTemplatesPage` (the third item under the Settings nav heading) is a per-record `CrudSection` list whose deletes already confirm, not a singleton "Save" button, so it was left out of scope; both pages now split their save handler into a validating `requestSave` and the actual mutation in `confirmSave`, gated behind the existing `ConfirmDialog` component (same reuse pattern as `CategorizationRulesPage`'s "Apply to history"); each dialog's message states the page-specific consequence — Tax: rate applies to all invoices/quotations from now on, existing ones keep their snapshotted rate; Business Details: since `InvoicePrintPage` reads business details live rather than snapshotting them, saving changes the letterhead on invoice/quotation PDFs including already-issued ones — flagged as the more consequential of the two; verified via tsc+lint and confirmed the built bundle contains both new confirmation strings after a container rebuild — no live browser click-through this session, no browser-automation tool available)
- [x] In Finance section hide all of the pages for now in frontemnd and in FINANCE text show FINANCE (Hidden with light font color so it doesnt take too much attention) and when hover text should show. "Enable to see pages" (done: 2026-07-06 19:18, corrected 2026-07-06 19:37; `Sidebar.tsx`'s `NavItem`/`NavGroup` types gained an optional per-item `hidden` flag and a group-level `hiddenNote`; every item under the Finance heading is flagged `hidden` so none render; the heading itself stays normal weight/color and now carries a light-colored `(Hidden)` badge (`text-slate-600` next to the normal `text-slate-500` heading) with `title="Enable to see pages"` as the native hover tooltip — first pass dimmed the whole heading instead of adding a hoverable "(Hidden)" word, corrected per follow-up feedback; routes in `App.tsx` untouched (still directly reachable by URL); verified via tsc+lint and confirmed the "(Hidden)" and "Enable to see pages" strings are present in the rebuilt container's bundle — no live browser click-through this session, no browser-automation tool available)
- [x] In CATALOG section hide Products page for now and make the CATALOG (Hidden with light font color so it doesnt take too much attention) and when hober text should show "Products page is hidden, enable to show" (done: 2026-07-06 19:18, corrected 2026-07-06 19:37; implemented together with the Finance item above via the same `Sidebar.tsx` mechanism — only the "Products" item under Catalog is flagged `hidden` (Services and Car Makes & Models stay visible), while the "Catalog" heading gets the same light `(Hidden)` badge with `title="Products page is hidden, enable to show"` (corrected from the first pass's whole-heading dimming per follow-up feedback); route untouched, still reachable directly by URL; verified via tsc+lint and confirmed the "(Hidden)" and "Products page is hidden, enable to show" strings are present in the rebuilt container's bundle — no live browser click-through this session, no browser-automation tool available)
- [x] In my previous request I asked for catalog and FInance to have a Hidden word that is hoverable with the details I provided but right now there is nothing you just hide directly the pages inside the sections. eg: FINANCE (Hidden) (done: 2026-07-06 19:37; see the corrected Finance/Catalog entries directly above — replaced the whole-heading dimming with a literal `(Hidden)` word appended to the heading, styled in a lighter color and carrying the hover tooltip, matching the originally requested "FINANCE (Hidden)" look; verified via tsc+lint and confirmed the `(Hidden)` string is present in the rebuilt container's bundle — no live browser click-through this session, no browser-automation tool available)

# 2026-07-10

## General
- [x] Create role management page where admin is able to add new accounts and set the role (email confirmation is needed before account becomes active) and update roles of current accounts (done: 2026-07-10 21:17; backend: `EmailConfirmationCode` entity (mirrors the existing `PasswordChangeCode` code/hash/expiry pattern) + migration, new `AccountsController`/`IAccountAdminService`/`AccountAdminService` at `api/accounts` (`[Authorize(Roles = "Admin")]` except the anonymous `POST /accounts/confirm`) — `GET` lists every account+roles+active status, `POST` creates a login for an existing Employee with `EmailConfirmed = false` and emails a 6-digit code (rolls back the half-created account if the send fails), `PUT .../role` swaps roles with a guard against demoting the last remaining Admin, `POST /confirm` verifies the code and lets the new hire set their first password; `Program.cs` now sets `options.SignIn.RequireConfirmedEmail = true` and `AuthController.Login` surfaces a distinct "hasn't been confirmed yet" message instead of a generic invalid-credentials error; 11 new xUnit tests (all 434 backend tests passing at this point); frontend: new `AccountsPage` (Staff nav, Admin-only) to list accounts, create one (employee picker limited to employees without an account yet, editable login email, role select) and change a role inline, plus a public `/confirm-account` page for the emailed code + new password; verified against the live stack end-to-end via curl (create → confirm → log in with the new password; confirmed login is rejected before confirming; confirmed the last-Admin guard only blocks when it would actually leave zero Admins) using throwaway test employees/accounts cleaned up afterward — no live browser click-through this session, no browser-automation tool available)
- [x] for every update. like some detail got updated - there should be a tag who last updated(user's name) it and timestamp (done: 2026-07-10 21:17; backend: `BaseEntity` gained `UpdatedByUserId`/`UpdatedByName`, auto-stamped in `AppDbContext.SaveChangesAsync` from the signed-in request's claims (null for seeders/background jobs with no request) — applies to all 36 entities via one migration; a new `AppUserClaimsPrincipalFactory` adds the linked Employee's full name as a claim at sign-in so the stamp is a claims read, not a per-write DB join; `UpdatedByName` surfaced on 8 DTOs whose entities have a real "detail page" (Customer, Car, Job, Employee, Appointment, plus the Tax/BusinessDetails/EmailSettings singletons) — deliberately not swept across all 28+ DTOs/every CrudSection lookup table, scoped to where a user actually looks for "who changed this"; frontend: new shared `UpdatedByTag` component wired into `CustomerDetailPage`/`CarDetailPage`/`JobDetailPage`/`EmployeesPage` (a table column) and upgraded the existing bare "Last updated {date}" line on the three Settings pages to include the name; new `dateTime` formatter in `lib/format.ts`; 3 new xUnit tests covering the stamping logic directly (create doesn't stamp, update does, no-HttpContext leaves it null); verified live end-to-end via curl (create a customer → `updatedByName: null`, then update it → `updatedByName: "Admin User"`) against the real running stack, cleaned up afterward — no live browser click-through this session, no browser-automation tool available)
- [x] Move Reminder Templates to Catalog (Rename catalog as templates) (done: 2026-07-10 21:17; pure sidebar-nav reorg in `Sidebar.tsx` — renamed the "Catalog" heading to "Templates" and moved the "Reminder Templates" item into that group (was under "Settings"); route/permissions left untouched, only the nav location and heading text changed; confirmed via tsc+lint and the rebuilt bundle containing `heading:"Templates"` alongside `label:"Reminder Templates"` in the same group — no live browser click-through this session (pure rename/move, no new interactive behavior, exempted per the frontend CLAUDE.md's rule for copy/rename changes), no browser-automation tool available)
- [x] Only admin could change/update settings page (done: 2026-07-10 21:17; investigated first — Tax/BusinessDetails/EmailSettings were already fully Admin-gated end to end (frontend `RequireAdmin` + backend class-level `[Authorize(Roles = "Admin")]`); the real gap was `ReminderTemplatesController`, which had no authorization at all. Since reminder templates are read by every signed-in mechanic when picking a preset while adding a reminder on a job/car, gating the whole controller would have broken that everyday workflow — so only the create/update/delete endpoints got method-level `[Authorize(Roles = "Admin")]`, leaving GET open; `ReminderTemplatesPage.tsx` now hides the Add/Edit/Delete UI via `CrudSection`'s existing "omit onCreate/onUpdate/onDelete for read-only" support when `!isAdmin`. While verifying this live (real non-admin test account attempting a write), caught and fixed a genuine regression: the `AppUserClaimsPrincipalFactory` added earlier the same session inherited from the non-role-aware `UserClaimsPrincipalFactory<TUser>` base instead of the role-aware `<TUser, TRole>` one, silently dropping role claims from every login's auth cookie and breaking every `[Authorize(Roles = ...)]` check API-wide since that change — added 2 regression tests exercising the real claims principal (not just `UserManager.GetRolesAsync`, which uses a live DB lookup and masked the bug) and reconfirmed live with a fresh login that Admin-gated writes work again; all 436 backend tests passing after the fix; verified live end-to-end with a throwaway non-admin account: GET succeeded (200), POST correctly rejected (403), Admin POST succeeded (201) — cleaned up afterward)
- [x] Add filters for Customers, Job Center, Invoices, Quotations pages like their order, date and other common filters needed (done: 2026-07-10 21:17; backend: all three paged endpoints (`/customers/paged`, `/jobs/paged`, `/invoices/paged`) gained a `sortBy` param ("newest" default/"oldest", plus "name" for customers and "amountDesc"/"amountAsc" for invoices) and a real `dateFrom`/`dateTo` range filter on the created/issue date; Job Center also gets a `status` (JobStatus) filter, Invoices/Quotations get `status` ("Active"/"Accepted"/"Rejected") and `isPaid` filters; replaced the old hack where the invoices `search` param silently double-matched as an exact issue-date parse — now `search` is name/rego text only, and date filtering is a genuine range via `dateFrom`/`dateTo` (updated/added the corresponding xUnit tests, including removing the now-obsolete exact-date-in-search test); 14 new xUnit tests across the three services (all 445 backend tests passing); frontend: new shared `DateRangeFilter` component (two date inputs + clear) used on all three pages, plus a sort dropdown on each and a status dropdown on Job Center/Invoices/Quotations — filter state changes force a `CrudSection` refetch via its existing `reloadKey` prop; removed the now-fully-unused `dateSearchButton` prop and its `DateSearchButton` component from `CrudSection` (its only consumer was replaced by the new date-range UI); verified against the live stack with real production-like data (453 customers, real invoices/jobs) via curl across every new param on all three endpoints (sort orders, date ranges, job status, invoice status/paid) and confirmed the built frontend bundle contains the new dropdown strings — no live browser click-through this session, no browser-automation tool available)
- [x] When I created a new account, it sends a confirmation code to the email but no link or nowhere to use that code. instead of code when creating new account it should be a creation link where the new account would set their own password. (done: 2026-07-10 22:31; backend: replaced the emailed 6-digit code with a long random hex token (32 bytes, hashed the same way as before) embedded in a clickable activation link; new `Frontend:BaseUrl` config (appsettings.json + `docker-compose.yml`/`.env.example`, no such config existed anywhere before) builds the absolute `{baseUrl}/confirm-account?token=...` URL; `ConfirmAccountRequest` dropped `Email`/`Code` in favor of just `Token` (the token alone is now the lookup key — a direct hash-equality query, which is the standard safe approach for a long opaque token, unlike the old short-code+email-anchor design); renamed `AccountAdminError.InvalidCode/CodeExpired` to `InvalidToken/TokenExpired`; added a new anonymous `GET /api/accounts/confirm/{token}` preview endpoint so the confirm page can show whose account it's activating before asking for a password; 2 new/updated xUnit tests plus a new `GetInvitePreviewAsync` test (447 backend tests passing); frontend: `ConfirmAccountPage` now reads `token` from the URL, fetches and shows the invite preview (name + email), and only asks for a new password (with a confirm-password field) — no more manual email/code entry; verified live end-to-end against the real stack (created a throwaway test account, injected a known token hash via SQL the same way the code-based flow was tested last session since the real email can't be read in this environment, hit the preview endpoint, confirmed, and logged in with the new password) — no live browser click-through this session, no browser-automation tool available)
- [x] In Accounts and roles, admins should not be able to edit their own role (done: 2026-07-10 22:31; `AccountAdminService.UpdateRoleAsync` gained an `actingUserId` parameter and now fails with a new `CannotEditOwnRole` error before any other check when `userId == actingUserId` — checked ahead of the existing "last admin" guard so it applies unconditionally, even when a second Admin exists; `AccountsController` resolves the acting user via `UserManager.GetUserId(User)` (same pattern as the self-service `AccountController`) and maps the new error to a 400 with a clear message; frontend: `AccountsPage`'s per-row role `<select>` is replaced with a plain (non-editable) label + explanatory tooltip on the signed-in user's own row, comparing `account.userId` against `useAuth().user.id`; 1 new xUnit test (a second Admin exists so it isn't the "last admin" path, isolating that this really is the new self-edit guard); all 448 backend tests passing; verified live end-to-end — PUT on the signed-in admin's own account returned 400 "You can't change your own role", confirmed the frontend bundle contains the new guard message)
- [x] For Invoices and Quotations page, add a clear button so when user used a filter it can be easily cleared to show all (done: 2026-07-10 22:31; pure frontend, no backend change — `DocumentListPage` (shared by both `/invoices` and `/quotations`) now shows a "Clear filters" text link next to the date-range filter, visible only when `sortBy`/`statusFilter`/`dateFrom`/`dateTo` differ from their defaults, resetting all four in one click; matches the existing "Show all" search-clear link's style for visual consistency; scoped to Invoices/Quotations only, as asked — Customers/Job Center weren't touched; verified via tsc+lint and confirmed the "Clear filters" string is present in the rebuilt frontend bundle — no live browser click-through this session, no browser-automation tool available)
- [x] admins should be able to make accounts inactive - deleted after 30 days (done: 2026-07-10 23:40; backend: new `ApplicationUser.DeactivatedAtUtc` (DateTime?, migration `AddApplicationUserDeactivatedAt`) — deliberately separate from `EmailConfirmed`, since "deactivated" and "invite still pending" are different states that could in principle overlap; `AccountAdminService.DeactivateAsync` sets it plus Identity's own `LockoutEnd = DateTimeOffset.MaxValue` (blocks sign-in immediately via the existing lockout check, no new auth logic needed) — guarded the same way as the role-edit task: can't deactivate yourself, can't deactivate the last remaining Admin; `ReactivateAsync` clears both; new hourly `AccountPurgeJob` (`BackgroundService`, mirrors the existing `RecurringTransactionPostingJob` hosted-service pattern exactly) hard-deletes any account deactivated 30+ days ago via a new `PurgeExpiredDeactivatedAccountsAsync`, also cleaning up its now-orphaned `EmailConfirmationCode`/`PasswordChangeCode` rows first (neither has a real FK to `AspNetUsers`, so nothing would otherwise stop the user delete but they'd be left stale forever); `AuthController.Login` now tells a deactivated user plainly that their account was deactivated rather than the misleading generic "locked out, try again later" (both conditions map to Identity's same `IsLockedOut` result, so this needed an explicit check against `DeactivatedAtUtc`); while writing tests, found and fixed a real gap in the existing `AccountAdminServiceTests` harness — `db` and `userManager`/`roleManager` were backed by two separate `AppDbContext` instances pointed at the same in-memory database name (worked by accident until a test needed to load an entity via one and delete it via the other, which threw EF's "already tracked by a different instance" error); fixed by registering the exact `db` instance in the test's DI container instead of a second `AddDbContext`, matching production's single-shared-scoped-context reality — this fix benefits every test in the file, not just the new ones; 9 new xUnit tests (455 backend tests passing); frontend: `AccountsPage` gained an Actions column with a "Deactivate" button (behind a `ConfirmDialog` spelling out the 30-day consequence) or "Reactivate", hidden for the signed-in user's own row same as the role guard; the Status column shows "Deactivated — deletes in Nd" (computed client-side from `deactivatedAtUtc`) ahead of the existing Active/Pending states; verified live end-to-end against the real stack with a throwaway test account: login succeeded before deactivation, got the new "deactivated — contact an Admin" message after, reactivation restored login, self-deactivate and last-admin attempts both correctly rejected with 400 — cleaned up afterward; no live browser click-through this session, no browser-automation tool available)
